This is the repository for the CS 4640 project.

To update Angular components on CS4640 server:
- /dist/ is included in .gitignore by default, but is included in the commits for this repository as it is used to deploy the Angular component.
- In order to link frontend php to Angular component:
  - Run ng build.
  - Then link to /dist/angular/index.html for the Angular component.
